task build {
    dependsOn 'clean'
    dependsOn 'client:build'
    dependsOn 'server:build'
    doLast() {
        copy {
            from 'server/build/libs'
            into 'build'
        }
    }
}

task clean {
    doLast {
        delete 'build'
    }
}