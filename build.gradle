task build {
    dependsOn 'clean', 'client:build', 'server:build'
    doLast() {
        copy {
            from 'server/build/libs'
            into 'build'
        }
    }
}

task stage(dependsOn: 'build')

task clean {
    doLast {
        delete 'build'
    }
}